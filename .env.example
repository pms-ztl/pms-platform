# ═══════════════════════════════════════════════════════════
# PMS Platform — Environment Configuration
# ═══════════════════════════════════════════════════════════
#
# SETUP: This file is auto-copied to .env, apps/api/.env,
# and packages/database/.env during `npm run setup`.
# You only need to edit this ONE file, then re-run setup.
#
# API KEYS: Replace the placeholder values below with your
# own keys. Only SMTP + at least one AI key are needed for
# full functionality. Everything else works out of the box.
# ═══════════════════════════════════════════════════════════

# ── Application ───────────────────────────────────────────
NODE_ENV=development
PORT=3001
HOST=0.0.0.0
APP_URL=http://localhost:3002

# ── Database (PostgreSQL via Docker) ──────────────────────
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5433/pms_dev

# ── Redis (via Docker) ────────────────────────────────────
REDIS_URL=redis://127.0.0.1:6379

# ── Authentication ────────────────────────────────────────
JWT_SECRET=pms-development-jwt-secret-key-32chars
JWT_EXPIRES_IN=8h
JWT_REFRESH_EXPIRES_IN=7d
SESSION_SECRET=pms-development-session-secret-32ch

# ── Encryption (AES-256, exactly 32 characters) ──────────
ENCRYPTION_KEY=pms-dev-encryption-key-32chars!!

# ── CORS ──────────────────────────────────────────────────
CORS_ORIGINS=http://localhost:3000,http://localhost:3002,http://localhost:4173,http://localhost:5173

# ── Logging ───────────────────────────────────────────────
LOG_LEVEL=debug

# ── MFA (TOTP) ────────────────────────────────────────────
MFA_ISSUER=PMS Platform

# ═══════════════════════════════════════════════════════════
# OPTIONAL — Add your own keys below to enable features
# ═══════════════════════════════════════════════════════════

# ── SMTP Email (required for invitations & alerts) ────────
# Get a Gmail App Password: https://myaccount.google.com/apppasswords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-gmail-app-password
SMTP_FROM=PMS Platform <your-email@gmail.com>

# ── AI / LLM Providers (set at least one for AI features) ─
# Groq (free, fast): https://console.groq.com/keys
GROQ_API_KEY=your-groq-api-key

# Google Gemini (free tier): https://aistudio.google.com/apikey
GEMINI_API_KEY=your-gemini-api-key

# Choose primary AI provider: groq | gemini
AI_PRIMARY_PROVIDER=groq
AI_MAX_TOKENS=4096
